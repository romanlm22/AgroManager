spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/agromanager
    username: agromanager
    password: agromanager

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration